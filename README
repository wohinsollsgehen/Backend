# Backend
This is the awesome backend for the #HackFr19

## Setup

1. Checkout this repo
2. Generate the local database `python3 generateDB.py`
3. Run the script `python3 main.py`

## Configure API endpoints

1. Install [Postman!](getpostman.com)
2. Import collection from repo `configure_backend.postman_collection.json`
3. List all sources without location metadata
4. Set location metadata for one source
